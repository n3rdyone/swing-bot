# Contributing

All active development happens on the `develop` branch. Please branch off `develop` and open PRs back into `develop`. The `main` branch receives merges from `develop` only when a release is ready.

## Workflow

```
feature/your-feature  →  develop  →  main
```

## Running tests

```bash
pytest tests/
```
